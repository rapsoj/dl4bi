cls: GP
kwargs: 
  kernel:
    func: periodic
  var:
    cls: Prior
    kwargs:
      dist: fixed
      kwargs:
        value: 1.0
  ls:
    cls: Prior
    kwargs:
      dist: beta
      kwargs:
        a: 3.0
        b: 7.0
  period:
    cls: Prior
    kwargs:
      dist: fixed
      kwargs:
        value: 2.0 # ~3 cycles on unit interval (2*pi/3)
